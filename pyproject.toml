[build-system]
requires = ["setuptools>=61.0", "setuptools_scm[toml]>=6.2"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["DQN"]
py-modules = ["custom_utils"]

[project]
name = "DQN"
# version = "0.0.1", version is determined by setuptools_scm
dynamic = ["version"]
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "numpy",
    "gymnasium[classic-control,other]>=0.29.1,<1.1.0",
    "scikit-learn",
    "torch>=2.4.0",
    "opencv-python>=4.6.0",
]

[tool.setuptools_scm]
write_to = "DQN/_version.py"

[project.urls]
"Homepage" = "https://github.com/caochengrui/DQN"
"Bug Tracker" = "https://github.com/caochengrui/DQN/issues"

[tool.ruff]
# Same as Black.
line-length = 127
# Assume Python 3.8
target-version = "py38"

[tool.ruff.lint]
# See https://beta.ruff.rs/docs/rules/
select = ["E", "F", "B", "UP", "C90", "RUF"]
# Ignore explicit stacklevel`
ignore = ["B028"]

[tool.lint.ruff.per-file-ignores]


[tool.lint.ruff.mccabe]
# Unlike Flake8, default to a complexity level of 10.
max-complexity = 15

[tool.black]
line-length = 127


[tool.mypy]
ignore_missing_imports = true
follow_imports = "silent"
show_error_codes = true
# exclude = """(?x)(
#     stable_baselines3/common/buffers.py$
#   )"""
# plugins = ["numpy.typing.mypy_plugin"]
# disable_error_code = "assignment"
